# 0716 InternNav 本机推理与 Nav2 Dry-run 测试总结

## 最新结果

- 主相机已切换为 `/moz_robot/camera/cam_high_extra/image_raw`，辅助监控相机为 `/moz_robot/camera/cam_high/image_raw`。
- 本机 HTTP 服务已重载，模型为 `/workspace/model_vln`、设备为 `cuda:0`，`/health` 返回 200。
- `/eval_dual` 已加入互斥锁，并发测试确认模型 `reset/step` 最大并发数为 1。
- 真实主相机完成 5/5 次 HTTP 200 推理：1 次返回左转 `[2, 2, 2, 2]`，4 次返回 33 点轨迹。
- 输入与完整结果保存在 `experiment_records/20260716_local_inference_5/`。
- 新 dry-run 客户端会发布 `/internnav/predicted_path` 与 `/internnav/status`，但 dummy depth 下永远不发送 FollowPath、Spin 或速度指令。
- 隔离 ROS domain 闭环测试通过：合成相机、odom、TF 后发布了 `moz1/map` Path，且 `goal_sent=false`、无 `/cmd_vel` 或 `/mx_base_vel_command` 发布器。
- 轨迹/动作、HTTP 并发和 ROS 闭环新增测试共 11 项，全部通过。

## Nav2 本地 Overlay

- `nav/build_local_nav.sh` 已成功构建 `control_bridge`、`navigation_interfaces`、`nav2_simple_commander`、`nav2_bringup`。
- 四个包均解析到 `/workspace/Intern_g1/nav/install`。
- `bringup_spirit_glim_launch.py` 新增 `start_nav_executor` 参数；InternNav 统一启动时设为 `false`。
- 新增统一预检、启动和停止脚本：
  - `scripts/realworld/preflight_internnav_nav.sh`
  - `scripts/realworld/start_internnav_nav.sh`
  - `scripts/realworld/stop_internnav_nav.sh`

## 当前实机阻塞

统一启动已验证会安全拒绝当前共享 ROS 图，不会拉起第二套导航。当前仍有：

- `/vln_node`
- 2 个 `/message_forward`
- 4 个 `/nav_executor`
- 2 个 `/lifecycle_manager_slam`
- 辅助相机当前无新帧
- `/moz1/odom_global` 无可测频率
- `moz1/map -> moz1/base_link` TF 不可用

这些节点位于容器外的共享 ROS 图中，不能通过容器内 PID 精确清理。宿主机旧 launch 未停止且 odom/TF 未恢复前，生产客户端保持拒绝启动/HOLD。

## 当前命令

```bash
cd /workspace/Intern_g1
nav/build_local_nav.sh
scripts/realworld/start_internnav_nav.sh
```

```bash
scripts/realworld/stop_internnav_nav.sh
```

## 初始阶段历史记录（以下内容已被上方最新结果取代）

在当前 Docker 容器内完成 InternNav 项目安装、InternVLA-N1 HTTP 推理服务启动，并使用 ROS 相机话题进行低频客户端推理测试。

## 环境信息

- 项目路径：`/workspace/Intern_g1`
- 模型路径：`/workspace/model_vln`
- HTTP 服务端口：`5801`
- 推理接口：`http://localhost:5801/eval_dual`
- 健康检查：`http://localhost:5801/health`
- 默认 ROS RGB 话题：`/camera/camera/color/image_raw`
- GPU：`cuda:0`

## 代码与脚本修改

- `docker_start_server_in_container.sh`
  - 自动定位项目目录，优先使用 `/workspace/Intern_g1`，兼容 `/workspace/InternNav`。
  - 启动前导出 HPCX 库路径，解决 `torch` 导入时 UCC/UCX 符号冲突：
    ```bash
    export LD_LIBRARY_PATH=/opt/hpcx/ucx/lib:/opt/hpcx/ucc/lib:${LD_LIBRARY_PATH:-}
    ```
  - 导出 `PYTHONPATH` 兜底源码导入：
    ```bash
    export PYTHONPATH="$PROJECT_DIR:${PYTHONPATH:-}"
    ```
  - 安装 `numpy-quaternion`。
  - 使用 `pip3 install -e . --no-deps --ignore-requires-python`，绕过 Python `3.12.3` 与项目 `python_requires <=3.12` 的版本声明冲突。
  - 不安装 `flash_attn`，不安装 `requirements/internvla_n1.txt`。

- `scripts/realworld/http_internvla_server.py`
  - 增加 `GET /health` 和 `GET /`。
  - 返回字段包括 `status`、`model_path`、`device`、`uptime`。
  - 保持 `POST /eval_dual` 协议不变。

- `scripts/realworld/run_simple_client.sh`
  - 健康检查改为访问 `/health`。
  - 保持 `SERVER_URL`、`RGB_TOPIC`、`FPS`、`INSTRUCTION` 环境变量接口不变。

- `scripts/realworld/simple_client.py`
  - 输出服务端真实返回字段：`trajectory`、`discrete_action`、`pixel_goal`。
  - `trajectory` 只打印长度和前 3 个点，避免日志过大。

## 验证结果

### 1. 安装与导入验证

执行内容：

```bash
cd /workspace/Intern_g1
export LD_LIBRARY_PATH=/opt/hpcx/ucx/lib:/opt/hpcx/ucc/lib:$LD_LIBRARY_PATH
pip3 install numpy-quaternion
pip3 install -e . --no-deps --ignore-requires-python
python3 -c "import torch; from internnav.agent.internvla_n1_agent_realworld import InternVLAN1AsyncAgent; print(torch.cuda.is_available())"
```

结果：

- `numpy-quaternion` 已安装。
- `internnav` editable install 成功。
- `torch` 可正常导入。
- `InternVLAN1AsyncAgent` 可正常导入。
- `torch.cuda.is_available()` 返回 `True`。

### 2. HTTP 推理服务

服务启动命令：

```bash
cd /workspace/Intern_g1
bash docker_start_server_in_container.sh > /tmp/internnav_server.log 2>&1 &
```

初始阶段服务状态（PID 已失效）：

- wrapper PID：`151064`
- server PID：`151488`
- 服务仍在运行。
- `/health` 返回 HTTP 200。

健康检查示例：

```json
{"device":"cuda:0","model_path":"/workspace/model_vln","status":"ok","uptime":1328.8847002983093}
```

### 3. HTTP 最小推理测试

执行命令：

```bash
python3 test_inference_simple.py \
  --url http://localhost:5801/eval_dual \
  --instruction "Walk forward to the door"
```

结果：

- HTTP 状态码：`200`
- 推理成功。
- 返回结果包含离散动作：
  ```text
  [2, 2, 2, 2]
  ```

结论：HTTP 推理链路已打通。

### 4. ROS 相机客户端测试

执行命令：

```bash
timeout 120 bash -lc 'RGB_TOPIC=/camera/camera/color/image_raw FPS=1.0 INSTRUCTION="Walk forward to the door" bash scripts/realworld/run_simple_client.sh'
```

结果：

- `/camera/camera/color/image_raw` 话题存在。
- 120 秒内客户端未收到 RGB 图像数据。
- 客户端一直等待图像，最终 timeout。
- 复核 `ros2 topic hz /camera/camera/color/image_raw`，5 秒内无频率输出。

结论：ROS 客户端未能完成推理，阻塞点是相机话题存在但没有图像帧发布。

## 初始阶段结论

- InternNav 项目安装问题已解决。
- `torch` / CUDA / `InternVLAN1AsyncAgent` 导入链路已解决。
- InternVLA-N1 HTTP 服务已成功启动并保持运行。
- HTTP 推理接口 `/eval_dual` 已验证可用。
- ROS 客户端链路阻塞在相机数据源：话题存在，但无图像消息。

## 后续建议

1. 启动或修复相机驱动，确认 RGB 话题有帧：
   ```bash
   ros2 topic hz /camera/camera/color/image_raw
   ```

2. 如果实际图像话题不是默认话题，列出可用图像话题：
   ```bash
   ros2 topic list | grep image
   ```

3. 确认有图像帧后重新运行客户端：
   ```bash
   RGB_TOPIC=/camera/camera/color/image_raw \
   FPS=1.0 \
   INSTRUCTION="Walk forward to the door" \
   bash scripts/realworld/run_simple_client.sh
   ```

4. 如需停止当前 HTTP 服务：
   ```bash
   kill 151488 151064
   ```
