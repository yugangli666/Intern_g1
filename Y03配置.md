# Y03 配置与无运动 VLN 闭环测试记录

更新时间：2026-07-23

## 本次目标

在 `internnav_server2` / `MOZ1-03-Y` 环境中完成 InternNav Sofa 指令的实机在线、机器人不运动 VLN 闭环测试，只验证：

```text
实时相机图像 -> VLN 模型推理 -> 输出解析 -> 控制计算 -> dry-run 输出
```

固定测试指令：

```text
Move to the sofa and stop in front of it.
```

硬安全边界：

```bash
ENABLE_MOTION=0
ALLOW_FORWARD_MOTION=0
MAX_MOTION_STEPS=0
```

测试期间禁止向 `/cmd_vel`、`/cmd_vel_nav`、`/cmd_vel_smoothed`、`/cmd_vel_teleop`、`/mx_base_vel_command` 发布任何实机运动命令。

## 话题选择

### 相机输入

最终使用：

```text
/moz_robot/camera/cam_high/image_raw
```

选择依据：

- 类型：`sensor_msgs/msg/Image`
- 分辨率：`1408x1280`
- 编码：`bgr8`
- 频率：约 `10 Hz`
- 有真实 publisher：`moz_robot_camera_publisher`
- 可连续更新，适合作为在线 VLN 输入

备用监控相机：

```text
/moz_robot/camera/cam_high_extra/image_raw
```

未选用的话题：

- `/camera/camera/color/image_raw`：当前无 publisher
- `/image`：当前无 publisher
- `/moz1/keyframe_image`：不是连续实时相机流

### Odom 输入

最终使用：

```text
/moz1/odom_global
```

选择依据：

- 类型：`nav_msgs/msg/Odometry`
- 频率：约 `200 Hz`
- `header.frame_id=moz1/map`
- `child_frame_id=moz1/base_link`
- 四元数有效且归一
- 静止状态下抖动为毫米级
- `moz1/map -> moz1/base_link` TF 可用

## 本次代码改动

### 1. `scripts/realworld/internnav_direct_control_client.py`

已支持无运动 dry-run 隔离发布：

- `enable_motion=False` 时，预览速度发布到：

```text
/internnav/dry_run_cmd_vel
```

- dry-run stop / emergency 测试信号发布到：

```text
/internnav/dry_run_stop_task
/internnav/dry_run_emergency_stop
```

- 不再向实际底盘控制链路发布非零速度。
- 增强 `events.jsonl` 日志字段：
  - `dry_run`
  - `image_topic`
  - `odom_topic`
  - `pose_x / pose_y / pose_yaw_rad`
  - `raw_discrete_action`
  - `trajectory_present / trajectory_point_count`
  - `pixel_goal_raw / pixel_goal_uv / pixel_goal_valid`
  - `computed_linear_x / computed_angular_z`
- 修复 HTTP 推理失败时不计入 `max_inferences` 的问题，避免服务端持续 500 时客户端无限重试。

### 2. `scripts/realworld/run_vln_no_motion_closed_loop.sh`

新增/完善完整五步测试流程：

1. 话题预检，生成 `topic_report.json` 和 `topic_report.md`
2. 推理服务健康检查，必要时自动启动服务
3. 实机控制话题 3 秒安全采样
4. 启动 no-motion 客户端并录制 rosbag
5. 后处理 rosbag，生成 `summary.json` 和安全验证报告

本次修复项：

- 修复 `timeout ros2 topic hz ... | grep` 在 `set -o pipefail` 下误判相机无数据的问题。
- 删除无效参数 `--enable-motion=false`，因为客户端使用 `--enable-motion` 的 `store_true` 语义，默认即为无运动。
- 客户端非零退出时不再直接中断脚本，保证 rosbag 停止、后处理和安全验证仍会执行。

### 3. `scripts/realworld/start_no_motion_test.sh`

新增快速启动入口，默认配置：

```bash
INSTRUCTION="Move to the sofa and stop in front of it."
MAX_INFERENCES=6
INFERENCE_FPS=0.2
```

运行命令：

```bash
bash scripts/realworld/start_no_motion_test.sh
```

也支持覆盖指令：

```bash
bash scripts/realworld/start_no_motion_test.sh "Move to the kitchen table"
```

### 4. `internnav/agent/internvla_n1_agent_realworld.py`

修复 eager 回退语义：

- 之前 `INTERNNAV_ALLOW_EAGER_ATTN=1` 只在 `flash-attn` import 失败时回退。
- 本次发现 `flash-attn` 能 import，但实际推理时报：

```text
CUDA error: no kernel image is available for execution on the device
```

- 现在设置 `INTERNNAV_ALLOW_EAGER_ATTN=1` 时会显式强制：

```text
attn_implementation=eager
```

该改动只影响模型 attention 后端，不影响运动控制链路。

## 已完成测试

成功测试目录：

```text
/workspace/Intern_g1/experiment_records/vln_no_motion_20260723_113148
```

运行方式：

```bash
INTERNNAV_ALLOW_EAGER_ATTN=1 bash scripts/realworld/start_no_motion_test.sh
```

测试结果：

- 客户端退出码：`0`
- 推理次数：`6`
- 推理频率配置：`0.2 Hz`
- 实际输入图像：实时 ROS 相机帧
- 实际 odom：`/moz1/odom_global`
- rosbag：已保存
- 安全验证：通过

模型输出统计：

```text
trajectory: 6/6
pixel_goal: 1/6
discrete_action: 0/6
STOP: 0/6
```

控制解析结果：

- 6 次输出均解析为 `trajectory_forward`
- 由于 `ALLOW_FORWARD_MOTION=0`，全部进入 HOLD
- dry-run 预览速度：

```text
linear.x = 0.0
linear.y = 0.0
angular.z = 0.0
```

安全验证结果：

```text
/cmd_vel: 全程零速度
/cmd_vel_nav: 全程零速度
/cmd_vel_smoothed: 全程零速度
/mx_base_vel_command: 全程零速度或无非零样本
```

`summary.json` 关键结果：

```json
{
  "max_twist_commands": {
    "/cmd_vel_nav": {"linear": 0.0, "angular": 0.0},
    "/cmd_vel_smoothed": {"linear": 0.0, "angular": 0.0},
    "/cmd_vel": {"linear": 0.0, "angular": 0.0}
  },
  "max_base_command": {"linear": 0.0, "angular": 0.0}
}
```

rosbag 统计：

```text
/moz1/odom_global: 7124 条
/internnav/status: 73 条
/internnav/dry_run_cmd_vel: 117 条
/internnav/dry_run_stop_task: 6 条
```

## 结果文件

成功测试目录包含：

```text
README.md
client.log
events.jsonl
metadata.json
summary.json
summary.log
rosbag/
topic_report.json
topic_report.md
safety_preflight.log
safety_verification.log
startup.log
inference_server.log
input_000001.jpg ... input_000006.jpg
response_000001.json ... response_000006.json
annotated_000001.jpg ... annotated_000006.jpg
```

其中 `annotated_*.jpg` 为补生成的关键帧标注图，包含：

- `DRY RUN / NO MOTION` 标识
- VLN 指令
- 解析动作和 HOLD 原因
- dry-run 速度预览
- `pixel_goal` 标注
- base-frame trajectory 小窗预览

## 当前已知问题

1. `flash-attn` 当前能 import，但运行时 CUDA kernel 与本机 GPU 架构不匹配，需要重新按当前 GPU 架构编译；在此之前使用：

```bash
INTERNNAV_ALLOW_EAGER_ATTN=1
```

2. 当前 `control_mode=pulse` 且 `ALLOW_FORWARD_MOTION=0`，因此虽然模型输出 trajectory，但控制预览被安全锁定为 HOLD。若下一阶段要验证非零 dry-run 速度预览，可在仍不连接实机控制链路的前提下增加“仅 dry-run 允许计算非零 preview 速度”的专用开关。

3. 当前没有目标检测器，`target_lock_semantics=unverified_without_object_detector`，因此 pixel/trajectory 是否真正指向 sofa 只能通过图像和人工检查判断。

## 下一步建议

1. 保持 no-motion 模式，先修复 `flash-attn` CUDA 架构问题，并用非 eager 再跑一次同样测试。
2. 增加 dry-run preview 专用非零速度计算模式，仍只发布到 `/internnav/dry_run_cmd_vel`，不接入实机控制链路。
3. 对 `annotated_*.jpg` 做人工检查，确认 sofa 目标和 trajectory 是否合理。
4. 只有在 no-motion 闭环稳定、速度隔离验证持续通过后，才进入低速实机运动阶段。
